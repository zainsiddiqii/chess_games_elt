# dagster_cloud.yaml

locations:
  - location_name: chess_data_elt # the name of the code location
    code_source:
      package_name: chess_data_elt # the name of the python package associated with the code location
